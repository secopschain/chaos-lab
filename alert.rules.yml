groups:
  - name: example
    rules:
      - alert: HighErrorRate
        expr: up == 1
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "Service is up"
          description: "Testing alert system"
